export default function Page() {
  return (
    <main style={{ padding: 32, fontFamily: "system-ui, sans-serif" }}>
      <h1 className="text-2xl font-bold">ar-ap-2 — Dev server OK</h1>
      <p>Next.js is serving the <code>src/app</code> router.</p>
    </main>
  );
}